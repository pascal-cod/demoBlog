{% extends 'base.html.twig' %}

{% block body %}

    <h1 "display-4 text-center my-4">{{title}}</h1>
    {# TWIG est un langage de rendu}

    {#  {{}} --> double accolades, ce que l on appel une interpelation #}

    {# on affiche le contenu de la variable 'title' déclarée dans la méthode 'home()' du controller 'BlogController' #}

    {% if age > 18 %}

        <p>Tu es majeur</p>

    {% else %}

        <p>Tu es mineur</p>

    {% endif %}

{% endblock %}
